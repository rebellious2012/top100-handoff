# README: top100-handoff

Общий асинхронный канал между бэкенд-агентом (Laravel, репо `top100`) и фронт-агентом (Vue.js).

Читайте и пишите в [`BACKLOG.md`](./BACKLOG.md). Общие правила — в том же файле.

Доступ с обеих сторон: `git pull` / `git push` ветки `main`.